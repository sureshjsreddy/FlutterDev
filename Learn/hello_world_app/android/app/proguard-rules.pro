# Flutter specific rules
    -keep class io.flutter.embedding.** { *; }

    # Add any other custom rules your app might need below
    # For example, if you use specific native libraries or reflection:
    # -keep class com.example.mycustomlibrary.** { *; }
    # -keep public class * extends android.view.View { ... }